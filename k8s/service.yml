apiVersion: v1
kind: Service
metadata:
  name: pauounny
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "pauounny"
    port: 8080
  selector:
    app: pauounny
